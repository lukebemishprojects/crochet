pluginManagement {
    repositories {
        gradlePluginPortal()
        // TODO: publish stuff
        mavenLocal()
    }

    includeBuild '../'
}

includeBuild '../'

rootProject.name = 'crochet-test'

include(':subproject')
include(':no-crochet')
