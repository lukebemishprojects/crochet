pluginManagement {
    repositories {
        gradlePluginPortal()
    }

    includeBuild '../'
}

includeBuild '../'

rootProject.name = 'crochet-test'

include(':subproject')
include(':no-crochet')
